# AndroidX support
android.useAndroidX=true
android.enableJetifier=true

# Build optimization
org.gradle.jvmargs=-Xmx2048m

# Kotlin
kotlin.code.style=official

# Android SDK path (from environment variable)
# Set ANDROID_SDK_ROOT before building: export ANDROID_SDK_ROOT=$HOME/Library/Android/sdk
